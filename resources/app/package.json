{
  "name": "subject-navigator",
  "version": "1.0.0",
  "description": "A tool to manage subject folders with a modern UI",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . SubjectNavigator --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "electron": "^32.1.2",
    "electron-store": "^10.0.0"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}